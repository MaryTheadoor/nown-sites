# hero modules

The opening, high-impact tiles.

| Tile | Purpose | Notes |
|---|---|---|
| `hero` | Full-width opening band | headline, subcopy, actions, optional media; `.tile-hero` |
| `cta` | Call-to-action band | near/full-width; prompts conversion |

Both are responsive via the auto-fit grid + container queries (`@container`).
